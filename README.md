# 📚 راهنمای کامل مطالعه پروژه LapShop

این فایل شامل توضیحات کامل تمام فایل‌ها و پوشه‌های پروژه فروشگاه آنلاین لپ‌تاپ است که برای مطالعه و درک ساختار برنامه طراحی شده است.

---

## 📁 ساختار کلی پروژه

```
lap_shop/
├── node_modules/          # پکیج‌های نصب شده npm
├── public/                # فایل‌های استاتیک
├── src/                   # کدهای اصلی برنامه
├── package.json           # فایل تنظیمات و وابستگی‌های پروژه
├── package-lock.json      # قفل نسخه‌های دقیق پکیج‌ها
├── postcss.config.js      # تنظیمات PostCSS
├── tailwind.config.js     # تنظیمات Tailwind CSS
└── README.md              # این فایل
```

---

## 📄 فایل‌های ریشه پروژه

### `package.json`
**مسیر:** `/package.json`

**توضیحات:**
- فایل اصلی تنظیمات پروژه Node.js/React
- شامل نام پروژه، نسخه، وابستگی‌ها (dependencies) و اسکریپت‌های اجرایی
- **وابستگی‌های اصلی:**
  - `react` و `react-dom`: کتابخانه‌های اصلی React
  - `react-router-dom`: مدیریت مسیرها و ناوبری
  - `react-icons`: آیکون‌های آماده
  - `chart.js` و `react-chartjs-2`: برای نمایش نمودارها در داشبورد
  - `react-quill`: ویرایشگر متن غنی برای توضیحات محصولات
- **اسکریپت‌ها:**
  - `npm start`: اجرای پروژه در حالت توسعه
  - `npm build`: ساخت نسخه تولید
  - `npm test`: اجرای تست‌ها

### `package-lock.json`
**مسیر:** `/package-lock.json`

**توضیحات:**
- فایل قفل نسخه‌های دقیق تمام پکیج‌ها و وابستگی‌های آن‌ها
- تضمین می‌کند که همه توسعه‌دهندگان نسخه‌های یکسانی نصب کنند
- به صورت خودکار توسط npm تولید می‌شود

### `tailwind.config.js`
**مسیر:** `/tailwind.config.js`

**توضیحات:**
- فایل تنظیمات Tailwind CSS
- تعریف رنگ‌ها، فونت‌ها، breakpoint‌ها و سایر تنظیمات سفارشی
- مسیرهای فایل‌های CSS و JavaScript برای purge کردن استایل‌های استفاده نشده

### `postcss.config.js`
**مسیر:** `/postcss.config.js`

**توضیحات:**
- تنظیمات PostCSS برای پردازش CSS
- معمولاً شامل پلاگین‌های Tailwind و Autoprefixer

---

## 📂 پوشه `public/`

### `public/index.html`
**مسیر:** `/public/index.html`

**توضیحات:**
- فایل HTML اصلی که React به آن mount می‌شود
- شامل تگ `<div id="root">` که نقطه ورود برنامه است
- متادیتاهای صفحه، لینک فونت‌ها و سایر منابع استاتیک

---

## 📂 پوشه `src/` - کدهای اصلی برنامه

### `src/index.js`
**مسیر:** `/src/index.js`

**توضیحات:**
- نقطه ورود اصلی برنامه React
- کامپوننت `App` را به DOM رندر می‌کند
- استفاده از `React.StrictMode` برای شناسایی مشکلات احتمالی
- وارد کردن فایل CSS اصلی (`index.css`)

### `src/index.css`
**مسیر:** `/src/index.css`

**توضیحات:**
- فایل CSS اصلی برنامه
- شامل استایل‌های عمومی و reset CSS
- معمولاً شامل دستورات Tailwind CSS (`@tailwind base`, `@tailwind components`, `@tailwind utilities`)

### `src/App.js`
**مسیر:** `/src/App.js`

**توضیحات:**
- کامپوننت اصلی برنامه که ساختار کلی را تعریف می‌کند
- **مسئولیت‌ها:**
  - تعریف تمام مسیرهای برنامه با React Router
  - پیاده‌سازی Context Providers (Auth, Product, Cart)
  - تقسیم‌بندی مسیرهای مشتری و ادمین
- **مسیرهای مشتری:**
  - `/` - صفحه اصلی
  - `/products` - لیست محصولات
  - `/product/:id` - جزئیات محصول
  - `/cart` - سبد خرید
  - `/checkout` - پرداخت
  - `/about` - درباره ما
  - `/contact` - تماس با ما
- **مسیرهای ادمین:**
  - `/admin/login` - ورود ادمین
  - `/admin/dashboard` - داشبورد
  - `/admin/products` - مدیریت محصولات
  - `/admin/products/new` - افزودن محصول
  - `/admin/products/edit/:id` - ویرایش محصول
  - `/admin/orders` - مدیریت سفارشات

---

## 📂 پوشه `src/contexts/` - مدیریت State با Context API

### `src/contexts/AuthContext.js`
**مسیر:** `/src/contexts/AuthContext.js`

**توضیحات:**
- مدیریت احراز هویت و وضعیت لاگین ادمین
- **State:**
  - `isAdmin`: وضعیت لاگین ادمین (boolean)
- **Functions:**
  - `login(username, password)`: ورود ادمین (نام کاربری: admin، رمز: admin123)
  - `logout()`: خروج ادمین
- **ذخیره‌سازی:** استفاده از `sessionStorage` برای حفظ وضعیت لاگین در طول session
- **Hook سفارشی:** `useAuth()` برای دسترسی به context در کامپوننت‌ها

### `src/contexts/CartContext.js`
**مسیر:** `/src/contexts/CartContext.js`

**توضیحات:**
- مدیریت سبد خرید کاربر
- **State:**
  - `cartItems`: آرایه محصولات در سبد خرید
- **Functions:**
  - `addToCart(product, quantity)`: افزودن محصول به سبد
  - `removeFromCart(productId)`: حذف محصول از سبد
  - `updateQuantity(productId, quantity)`: تغییر تعداد محصول
  - `clearCart()`: پاک کردن کامل سبد
- **Computed Values:**
  - `totalPrice`: مجموع قیمت کل سبد
  - `totalItems`: تعداد کل آیتم‌ها
- **ذخیره‌سازی:** استفاده از `localStorage` برای حفظ سبد خرید حتی بعد از بستن مرورگر
- **Hook سفارشی:** `useCart()` برای دسترسی به context

### `src/contexts/ProductContext.js`
**مسیر:** `/src/contexts/ProductContext.js`

**توضیحات:**
- مدیریت محصولات (CRUD operations)
- **State:**
  - `products`: آرایه تمام محصولات
  - `loading`: وضعیت بارگذاری
- **Functions:**
  - `addProduct(productData)`: افزودن محصول جدید
  - `editProduct(id, productData)`: ویرایش محصول موجود
  - `deleteProduct(id)`: حذف محصول
  - `getProductById(id)`: دریافت محصول بر اساس ID
  - `getFeaturedProducts()`: دریافت محصولات ویژه
  - `searchProducts(query)`: جستجوی محصولات
- **ذخیره‌سازی:** استفاده از `localStorage` برای حفظ محصولات
- **Hook سفارشی:** `useProducts()` برای دسترسی به context

---

## 📂 پوشه `src/data/` - داده‌های اولیه

### `src/data/products.js`
**مسیر:** `/src/data/products.js`

**توضیحات:**
- شامل داده‌های اولیه محصولات (seed data)
- آرایه `initialProducts` با نمونه محصولات لپ‌تاپ
- هر محصول شامل: id, name, brand, price, image, description, specs (RAM, CPU, Storage, etc.)
- در صورت نبود داده در localStorage، از این داده‌ها استفاده می‌شود

---

## 📂 پوشه `src/components/` - کامپوننت‌های قابل استفاده مجدد

### 📁 `src/components/common/` - کامپوننت‌های مشترک

#### `src/components/common/Layout.js`
**مسیر:** `/src/components/common/Layout.js`

**توضیحات:**
- Layout اصلی برای صفحات مشتری
- شامل Header و Footer
- استفاده از `<Outlet />` برای نمایش صفحات فرزند
- ساختار کلی صفحات عمومی را تعریف می‌کند

#### `src/components/common/Header.js`
**مسیر:** `/src/components/common/Header.js`

**توضیحات:**
- هدر اصلی سایت
- شامل:
  - لوگو و نام سایت
  - منوی ناوبری (صفحه اصلی، محصولات، درباره ما، تماس)
  - جستجوی محصولات
  - آیکون سبد خرید با نمایش تعداد آیتم‌ها
- لینک به صفحات مختلف با React Router

#### `src/components/common/Footer.js`
**مسیر:** `/src/components/common/Footer.js`

**توضیحات:**
- فوتر سایت
- شامل:
  - اطلاعات تماس
  - لینک‌های مفید
  - شبکه‌های اجتماعی
  - کپی رایت

#### `src/components/common/ProductCard.js`
**مسیر:** `/src/components/common/ProductCard.js`

**توضیحات:**
- کارت نمایش محصول در لیست محصولات
- نمایش:
  - تصویر محصول
  - نام و برند
  - قیمت
  - مشخصات کلیدی (RAM, CPU)
  - دکمه "افزودن به سبد"
- لینک به صفحه جزئیات محصول

#### `src/components/common/FilterSidebar.js`
**مسیر:** `/src/components/common/FilterSidebar.js`

**توضیحات:**
- سایدبار فیلتر محصولات
- فیلترها:
  - برند (Brand)
  - محدوده قیمت (Price Range)
  - مقدار RAM
  - نوع CPU
- استفاده از state برای نگهداری فیلترهای فعال
- اعمال فیلترها روی لیست محصولات

---

### 📁 `src/components/admin/` - کامپوننت‌های پنل مدیریت

#### `src/components/admin/AdminLayout.js`
**مسیر:** `/src/components/admin/AdminLayout.js`

**توضیحات:**
- Layout اصلی پنل مدیریت
- شامل:
  - محافظ مسیر (Route Guard) - بررسی لاگین ادمین
  - AdminSidebar برای منوی کناری
  - ناحیه محتوا با `<Outlet />`
- در صورت عدم لاگین، هدایت به صفحه لاگین

#### `src/components/admin/AdminSidebar.js`
**مسیر:** `/src/components/admin/AdminSidebar.js`

**توضیحات:**
- منوی کناری پنل مدیریت
- شامل:
  - لوگو/عنوان پنل
  - لینک‌های منو:
    - داشبورد
    - مدیریت محصولات
    - مدیریت سفارشات
  - دکمه خروج
- طراحی ریسپانسیو با قابلیت باز/بسته شدن در موبایل

#### `src/components/admin/ProductForm.js`
**مسیر:** `/src/components/admin/ProductForm.js`

**توضیحات:**
- فرم مشترک برای افزودن و ویرایش محصول
- فیلدها:
  - نام محصول
  - برند
  - قیمت
  - موجودی (Stock)
  - تصویر (URL)
  - توضیحات (با React Quill)
  - مشخصات (RAM, CPU, Storage, Screen, etc.)
  - محصول ویژه (Featured)
- اعتبارسنجی فرم
- استفاده مجدد در صفحات Add و Edit

---

## 📂 پوشه `src/pages/` - صفحات اصلی برنامه

### 📁 `src/pages/customer/` - صفحات مشتری

#### `src/pages/customer/HomePage.js`
**مسیر:** `/src/pages/customer/HomePage.js`

**توضیحات:**
- صفحه اصلی سایت
- شامل:
  - بنر/اسلایدر تبلیغاتی
  - بخش محصولات ویژه (Featured Products)
  - بخش دسته‌بندی‌ها
  - بخش آخرین محصولات
- استفاده از `getFeaturedProducts()` از ProductContext

#### `src/pages/customer/ProductsPage.js`
**مسیر:** `/src/pages/customer/ProductsPage.js`

**توضیحات:**
- صفحه لیست محصولات
- شامل:
  - FilterSidebar برای فیلتر کردن
  - نمایش محصولات در grid با ProductCard
  - جستجوی محصولات
  - مرتب‌سازی (بر اساس قیمت، نام، و...)
- مدیریت state برای فیلترها و جستجو

#### `src/pages/customer/ProductDetailPage.js`
**مسیر:** `/src/pages/customer/ProductDetailPage.js`

**توضیحات:**
- صفحه جزئیات محصول
- نمایش:
  - گالری تصاویر محصول
  - نام، برند، قیمت
  - توضیحات کامل
  - مشخصات فنی (RAM, CPU, Storage, Screen, etc.)
  - دکمه افزودن به سبد
  - انتخاب تعداد
- استفاده از `useParams()` برای دریافت ID از URL
- استفاده از `getProductById()` از ProductContext

#### `src/pages/customer/CartPage.js`
**مسیر:** `/src/pages/customer/CartPage.js`

**توضیحات:**
- صفحه سبد خرید
- نمایش:
  - لیست محصولات در سبد
  - تصویر، نام، قیمت هر محصول
  - امکان تغییر تعداد
  - دکمه حذف محصول
  - خلاصه سفارش (جمع کل، تعداد آیتم‌ها)
  - دکمه ادامه به پرداخت
- استفاده از `useCart()` برای دسترسی به سبد خرید

#### `src/pages/customer/CheckoutPage.js`
**مسیر:** `/src/pages/customer/CheckoutPage.js`

**توضیحات:**
- صفحه پرداخت و تکمیل سفارش
- شامل:
  - فرم اطلاعات مشتری (نام، آدرس، تلفن، ایمیل)
  - خلاصه سفارش
  - اعتبارسنجی فرم
  - دکمه ثبت سفارش
- پس از ثبت، سبد خرید پاک می‌شود و به صفحه تایید هدایت می‌شود

#### `src/pages/customer/AboutPage.js`
**مسیر:** `/src/pages/customer/AboutPage.js`

**توضیحات:**
- صفحه درباره ما
- شامل اطلاعات درباره فروشگاه، تاریخچه، تیم و...

#### `src/pages/customer/ContactPage.js`
**مسیر:** `/src/pages/customer/ContactPage.js`

**توضیحات:**
- صفحه تماس با ما
- شامل:
  - فرم تماس
  - اطلاعات تماس (آدرس، تلفن، ایمیل)
  - نقشه (در صورت نیاز)

---

### 📁 `src/pages/admin/` - صفحات پنل مدیریت

#### `src/pages/admin/AdminLogin.js`
**مسیر:** `/src/pages/admin/AdminLogin.js`

**توضیحات:**
- صفحه ورود ادمین
- شامل:
  - فرم لاگین (نام کاربری و رمز عبور)
  - اعتبارسنجی
  - نمایش پیام خطا در صورت ورود نامعتبر
- پس از لاگین موفق، هدایت به داشبورد
- استفاده از `login()` از AuthContext

#### `src/pages/admin/AdminDashboard.js`
**مسیر:** `/src/pages/admin/AdminDashboard.js`

**توضیحات:**
- داشبورد اصلی پنل مدیریت
- نمایش آمار:
  - تعداد کل محصولات
  - تعداد سفارشات
  - مجموع فروش
  - تعداد مشتریان
- نمودارها با Chart.js:
  - نمودار فروش در طول زمان
  - نمودار دسته‌بندی محصولات
- استفاده از داده‌های localStorage برای محاسبه آمار

#### `src/pages/admin/ProductsManagement.js`
**مسیر:** `/src/pages/admin/ProductsManagement.js`

**توضیحات:**
- صفحه مدیریت محصولات
- شامل:
  - جدول لیست تمام محصولات
  - نمایش: نام، برند، قیمت، موجودی
  - دکمه ویرایش برای هر محصول
  - دکمه حذف با تایید
  - دکمه افزودن محصول جدید
- استفاده از `useProducts()` برای دریافت و مدیریت محصولات

#### `src/pages/admin/AddProductPage.js`
**مسیر:** `/src/pages/admin/AddProductPage.js`

**توضیحات:**
- صفحه افزودن محصول جدید
- استفاده از کامپوننت `ProductForm`
- پس از ثبت موفق، هدایت به صفحه مدیریت محصولات
- استفاده از `addProduct()` از ProductContext

#### `src/pages/admin/EditProductPage.js`
**مسیر:** `/src/pages/admin/EditProductPage.js`

**توضیحات:**
- صفحه ویرایش محصول موجود
- دریافت ID از URL با `useParams()`
- بارگذاری اطلاعات محصول در فرم
- استفاده از کامپوننت `ProductForm` با حالت ویرایش
- استفاده از `editProduct()` از ProductContext

#### `src/pages/admin/OrdersManagement.js`
**مسیر:** `/src/pages/admin/OrdersManagement.js`

**توضیحات:**
- صفحه مدیریت سفارشات
- نمایش:
  - لیست تمام سفارشات
  - اطلاعات مشتری
  - محصولات سفارش
  - وضعیت سفارش (در انتظار، در حال پردازش، ارسال شده، تحویل داده شده)
  - امکان تغییر وضعیت
- ذخیره سفارشات در localStorage

---

### `src/pages/NotFoundPage.js`
**مسیر:** `/src/pages/NotFoundPage.js`

**توضیحات:**
- صفحه 404 برای مسیرهای ناموجود
- نمایش پیام خطا و لینک بازگشت به صفحه اصلی
- استفاده در Route با path="*" در App.js

---

## 🔄 جریان داده (Data Flow)

### 1. **مدیریت State:**
   - استفاده از Context API برای state management
   - سه Context اصلی: Auth, Cart, Product
   - ذخیره‌سازی در localStorage/sessionStorage

### 2. **مسیریابی:**
   - React Router DOM برای navigation
   - Protected Routes برای صفحات ادمین
   - Dynamic Routes برای صفحات محصولات

### 3. **جریان اطلاعات:**
   ```
   User Action → Component → Context → localStorage → UI Update
   ```

---

## 🛠️ تکنولوژی‌های استفاده شده

- **React 18.2.0**: کتابخانه اصلی UI
- **React Router DOM 6.20.0**: مدیریت مسیرها
- **Tailwind CSS 3.3.6**: استایل‌دهی
- **Context API**: مدیریت state
- **localStorage/sessionStorage**: ذخیره‌سازی داده‌ها
- **Chart.js**: نمودارها در داشبورد
- **React Icons**: آیکون‌ها
- **React Quill**: ویرایشگر متن غنی

---

## 📝 نکات مهم برای مطالعه

1. **Context API**: تمام state management با Context انجام می‌شود، نه Redux
2. **ذخیره‌سازی**: داده‌ها در localStorage ذخیره می‌شوند (نه backend)
3. **Protected Routes**: صفحات ادمین با بررسی `isAdmin` محافظت می‌شوند
4. **Component Reusability**: کامپوننت‌هایی مثل ProductCard و ProductForm قابل استفاده مجدد هستند
5. **Responsive Design**: تمام صفحات با Tailwind CSS ریسپانسیو طراحی شده‌اند

---

## 🚀 نحوه اجرای پروژه

```bash
# نصب وابستگی‌ها
npm install

# اجرای پروژه
npm start

# ساخت نسخه تولید
npm run build
```

---

## 🔐 اطلاعات ورود ادمین

- **نام کاربری:** `admin`
- **رمز عبور:** `admin123`

---

## 📚 ترتیب پیشنهادی برای مطالعه

1. شروع از `src/index.js` و `src/App.js` برای درک ساختار کلی
2. مطالعه Context ها (`src/contexts/`)
3. بررسی کامپوننت‌های مشترک (`src/components/common/`)
4. مطالعه صفحات مشتری (`src/pages/customer/`)
5. مطالعه صفحات ادمین (`src/pages/admin/`)
6. بررسی کامپوننت‌های ادمین (`src/components/admin/`)

---

**نکته:** این پروژه یک نمونه آموزشی است و برای استفاده در تولید نیاز به اتصال به Backend و API واقعی دارد.
